
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: volumereplicationgroups.ramendr.openshift.io
spec:
  group: ramendr.openshift.io
  names:
    kind: VolumeReplicationGroup
    listKind: VolumeReplicationGroupList
    plural: volumereplicationgroups
    shortNames:
    - vrg
    singular: volumereplicationgroup
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VolumeReplicationGroup is the Schema for the volumereplicationgroups
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: "VolumeReplicationGroup (VRG) spec declares the desired replication
              class and replication state of all the PVCs owned by a given application,
              which are identified via the given PVC label selector.  For every PVC
              created by the application, the VRG operator does the following: - Create
              a VolumeReplication (VR) CR to enable storage level replication \t of
              volume data and set the volume state to primary, secondary, etc. - Take
              the corresponding PV metadata in Kubernetes etcd and deposit it in \t
              the S3 store.  The url, access key and access id required to access
              the   S3 store is specified via environment variables of the VRG operator
              POD,   which is obtained from a secret resource. - Manage the lifecycle
              of VR CR and S3 data to match the CUD operations on \t VRG CR."
            properties:
              pvcLabelSelector:
                description: Label selector to identify all the PVCs that are in this
                  group This operator will create and needs to be replicated
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: A label selector requirement is a selector that
                        contains values, a key, and an operator that relates the key
                        and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: operator represents a key's relationship to
                            a set of values. Valid operators are In, NotIn, Exists
                            and DoesNotExist.
                          type: string
                        values:
                          description: values is an array of string values. If the
                            operator is In or NotIn, the values array must be non-empty.
                            If the operator is Exists or DoesNotExist, the values
                            array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: matchLabels is a map of {key,value} pairs. A single
                      {key,value} in the matchLabels map is equivalent to an element
                      of matchExpressions, whose key field is "key", the operator
                      is "In", and the values array contains only "value". The requirements
                      are ANDed.
                    type: object
                type: object
              volumeReplicationClass:
                description: ReplicationClass of all volumes in this replication group
                  that is passed to VolumeReplication CR
                type: string
              volumeState:
                description: Desired state of all volumes [primary or secondary] in
                  this replication group that is passed to VolumeReplication CR
                type: string
            required:
            - pvcLabelSelector
            - volumeReplicationClass
            - volumeState
            type: object
          status:
            description: 'VolumeReplicationGroupStatus defines the observed state
              of VolumeReplicationGroup INSERT ADDITIONAL STATUS FIELD - define observed
              state of cluster Important: Run "make" to regenerate code after modifying
              this file'
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
